id: kube proxy
name: Kube-proxy
full-link: /docs/reference/generated/kube-proxy
tags:
- fundamental
- core-object
short-description: >
  An Kubernetes network proxy that runs on each node in the cluster. Kube proxy is responsible for request forwarding.
long-description: >
  The kube proxy allows TCP and UDP stream forwarding or round robin TCP and UDP forwarding across a set of backend services.
