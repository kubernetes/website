apiVersion: v1
kind: Service
metadata:
  name: canary-demo-service
  labels:
    app: canary-demo
spec:
  type: ClusterIP
  selector:
    app: canary-demo
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
