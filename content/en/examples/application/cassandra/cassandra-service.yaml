apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: cassandra
  name: cassandra
spec:
  clusterIP: None
  ports:
  - port: 9042
  selector:
    app.kubernetes.io/name: cassandra
