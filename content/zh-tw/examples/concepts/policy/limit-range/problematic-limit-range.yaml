apiVersion: v1
kind: LimitRange
metadata:
  name: cpu-resource-constraint
spec:
  limits:
  - default: # 此處定義預設限制值
      cpu: 500m
    defaultRequest: # 此處定義預設請求值
      cpu: 500m
    max: # max 和 min 定義限制範圍
      cpu: "1"
    min:
      cpu: 100m
    type: Container
