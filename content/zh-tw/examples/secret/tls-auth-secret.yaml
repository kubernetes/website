apiVersion: v1
kind: Secret
metadata:
  name: secret-tls
type: kubernetes.io/tls
data:
  # 值爲 base64 編碼，這樣會掩蓋它們，但不會提供任何有用的機密性級別
  # 注意：將以下值替換爲你自己的base64編碼的證書和密鑰。
  tls.crt: "REPLACE_WITH_BASE64_CERT" 
  tls.key: "REPLACE_WITH_BASE64_KEY"