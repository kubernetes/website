apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql
  labels:
    app: mysql
data:
  primary.cnf: |
    # Apply this config only on the master.
    [mysqld]
    log-bin
  replica.cnf: |
    # Apply this config only on slaves.
    [mysqld]
    super-read-only

